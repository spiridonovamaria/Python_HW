# Напишите программу, которая принимает на вход цифру, обозначающую день недели, и проверяет, является ли этот день выходным.


check = "1234567"
num = input('Введите число от 1 до 7: ')
while len(num) != 1 or (num not in check):
    print('Нужно число от 1 до 7!')
    num = input('Введите число от 1 до 7: ')
num=int(num)
if num==6 or num==7:
    print(f'Да, {num} - это выходной день.')
else:
    print(f'Нет, {num} - это будний день.')